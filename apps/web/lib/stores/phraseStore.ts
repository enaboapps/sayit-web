// This file is deprecated and will be removed.
// Components should use Convex hooks directly instead:
//
// import { useQuery, useMutation } from 'convex/react';
// import { api } from '@/convex/_generated/api';
//
// Example usage:
// const phrases = useQuery(api.phrases.getPhrases);
// const boards = useQuery(api.phraseBoards.getPhraseBoards);
// const addPhrase = useMutation(api.phrases.addPhrase);
// const deletePhrase = useMutation(api.phrases.deletePhrase);
//
// See convex/phrases.ts and convex/phraseBoards.ts for available functions.

export const phraseStore = {
  // Deprecated - use Convex hooks directly
};
